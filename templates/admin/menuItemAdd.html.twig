{% extends 'admin/base.html.twig' %}

{% block title %}افزودن پیوند جدید{% endblock %}

{% block pageBody %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-8">
                {{ form_start(form) }}
                {{ form_label(form.label) }}
                {{ form_widget(form.label) }}
                {{ form_label(form.url) }}
                {{ form_widget(form.url) }}
                {{ form_label(form.fontawsome) }}
                {{ form_widget(form.fontawsome) }}
                <hr>
                <div class="row">
                    <div class="col-12">
                        {{ form_widget(form.submit) }}
                        <a class="btn btn-secondary" href="{{ path('adminMainMenu') }}">انصراف</a>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
