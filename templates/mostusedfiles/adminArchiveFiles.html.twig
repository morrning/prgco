{% extends 'mostusedfiles/base.html.twig' %}

{% block title %}آرشیو فرم ها{% endblock %}
{% block pageBody %}
    <div class="table-responsive">
        <table class="table table-sm table-bordered table-hover rounded text-center">
            <thead class="bg-blue text-light">
            <tr>
                <th>ردیف</th>
                <th>عنوان</th>
                <th>شناسه ایزو</th>
                <th>دسته بندی</th>
                <th>امکانات</th>
            </tr>
            </thead>
            <tbody class="">
            {% for file in files %}
                <tr class="">
                    <td>{{ loop.index }}</td>
                    <td>{{ file.title }}</td>
                    <td>{{ file.ISOCode }}</td>
                    <td>{{ file.cat.catName }}</td>
                    <td>
                        <a class="btn btn-success" href="{{ asset('files/' ~ file.fileID) }}">دریافت</a>
                        <a class="btn btn-sm btn-danger" href="{{ path('mostusedfilesDeleteFile',{'id':file.id}) }}">حذف</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}