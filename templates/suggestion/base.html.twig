{% extends 'appBase.html.twig' %}
{% block appName %}نظام پیشنهادات و انتقادات{% endblock %}
{% block appMenu %}
    <div class="d-none d-lg-block">
        <ul class="navbar-nav side-bg-btn py-3">
            <li class="nav-item side-bg-btn">
                <a class="nav-link side-btn" href="{{ path('suggestionAdmin') }}">
                    <i class="fas fa-chalkboard"></i>
                    <span>داشبورد</span>
                </a>
            </li>
        {% if UserMGR.hasPermission('suggestionInbox','SUGGESTION') == true %}
            <li class="nav-item side-bg-btn">
                <a class="nav-link side-btn" href="{{ path('suggestionInbox') }}">
                    <i class="fas fa-inbox"></i>
                    <span>صندوق دریافتی</span>
                </a>
            </li>
        {% endif %}
            <li class="nav-item side-bg-btn">
                <a class="nav-link side-btn" href="{{ path('suggestionReferralInbox') }}">
                    <i class="fas fa-list-alt"></i>
                    <span>آرشیو ارجاعات</span>
                </a>
            </li>
        </ul>
    </div>

    <div class="d-none d-sm-block d-md-block d-lg-none">
        <ul class="nav side-bg-btn py-3">
            <li class="nav-item side-bg-btn w-33">
                <a class="nav-link side-btn" href="{{ path('suggestionAdmin') }}">
                    <i class="fas fa-chalkboard"></i>
                    <span>داشبورد</span>
                </a>
            </li>
        {% if UserMGR.hasPermission('suggestionInbox','SUGGESTION') == true %}
            <li class="nav-item side-bg-btn w-33 border-right brd-color">
                <a class="nav-link side-btn" href="{{ path('suggestionInbox') }}">
                    <i class="fas fa-inbox"></i>
                    <span>صندوق دریافتی</span>
                </a>
            </li>
        {% endif %}
            <li class="nav-item side-bg-btn w-33 border-right brd-color">
                <a class="nav-link side-btn" href="{{ path('suggestionReferralInbox') }}">
                    <i class="fas fa-list-alt"></i>
                    <span>آرشیو ارجاعات</span>
                </a>
            </li>
        </ul>
    </div>

    <div class="d-block d-sm-none">
        <ul class="nav side-bg-btn py-3">
            <li class="nav-item side-bg-btn w-50 brd-color">
                <a class="nav-link side-btn" href="{{ path('suggestionAdmin') }}">
                    <i class="fas fa-chalkboard"></i>
                    <span>داشبورد</span>
                </a>
            </li>
        {% if UserMGR.hasPermission('suggestionInbox','SUGGESTION') == true %}
            <li class="nav-item side-bg-btn w-50 border-right brd-color">
                <a class="nav-link side-btn" href="{{ path('suggestionInbox') }}">
                    <i class="fas fa-inbox"></i>
                    <span>صندوق دریافتی</span>
                </a>
            </li>
        {% endif %}
            <li class="nav-item side-bg-btn w-50 border-right brd-color">
                <a class="nav-link side-btn" href="{{ path('suggestionReferralInbox') }}">
                    <i class="fas fa-list-alt"></i>
                    <span>آرشیو ارجاعات</span>
                </a>
            </li>
        </ul>
    </div>
{% endblock %}