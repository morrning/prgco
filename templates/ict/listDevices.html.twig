{% extends 'ict/baseDoing.html.twig' %}

{% block title %}لیست دستگاه ها و تجهیزات{% endblock %}
{% block pageBody %}
    <div class="table-responsive">
        <table class="table table-sm table-bordered table-hover rounded text-center">
            <thead class="thead-dark">
            <tr>
                <th>ردیف</th>
                <th>نوع دستگاه</th>
                <th>کد اموال</th>
                <th>تحویل گیرنده</th>
                <th>امکانات</th>
            </tr>
            </thead>
            <tbody class="">
            {% for machine in machines %}
                <tr class="">
                    <td>{{ loop.index }}</td>
                    <td>{{ machine.deviceType }}</td>
                    <td>{{ machine.ProdectCode }}</td>
                    <td>{{ machine.ownerID }}</td>
                    <td>
                        <a class="btn btn-primary" href="{{ path('ictDoingViewDevice',{'id':machine.id}) }}">مشاهده</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}