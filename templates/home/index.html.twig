{% extends 'appSimple.html.twig' %}

{% block title %}صفحه اصلی{% endblock %}

{% block pageBody %}
    <!-- Page Content -->
    <div class="d-none d-md-block">          {# MD-LG-XL screens #}
        <div class="row">
            <div class="col-12 col-sm-12 col-md-12 pb-2 px-0">
                <div class="bd-example">
                    <div id="carouselExampleCaptions" class="carousel slide" data-ride="carousel">
                        {#
                            <ol class="carousel-indicators">
                                <li data-target="#carouselExampleCaptions" data-slide-to="0" class="active"></li>
                                <li data-target="#carouselExampleCaptions" data-slide-to="1"></li>
                                <li data-target="#carouselExampleCaptions" data-slide-to="2"></li>
                            </ol>
                        #}
                        <div class="carousel-inner">
                            <div class="carousel-item active">
                                <img src="{{ asset('assets/images/slider/11.jpg') }}" class="d-block w-100" alt="...">

                                <div class="carousel-caption-2">
                                    <h5>نجف اشرف</h5>
                                    <p>صحن حضرت زهرا (س)</p>
                                </div>

                            </div>
                            <div class="carousel-item">
                                <img src="{{ asset('assets/images/slider/22.jpg') }}" class="d-block w-100" alt="...">

                                <div class="carousel-caption-2">
                                    <h5>کربلا</h5>
                                    <p>توسعه حرم مطهر امام حسین (ع)</p>
                                </div>

                            </div>
                            <div class="carousel-item">
                                <img src="{{ asset('assets/images/slider/33.jpg') }}" class="d-block w-100" alt="...">

                                <div class="carousel-caption-2">
                                    <h5>تبریز</h5>
                                    <p>توسعه منابع فیزیکی</p>
                                </div>

                            </div>
                        </div>
                        <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="sr-only">Previous</span>
                        </a>
                        <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="sr-only">Next</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="countdown">
                <div class="py-2 px-3 mb-1 bg-white text-right text-dark" style="font-size: .75rem;">
                    {% set countdowns = EntityMGR.findAll('App:Countdown') %}
                    <span class="fa fa-clock"></span>
                    امروز
                    <span>{{ Jdate.jdate('l , d/F/Y','now'|date('U')) }}</span>
                </div>
                <div class="card-body p-0">
                    <ul class="list-group m-0 p-0">
                        {% for countdown in countdowns %}
                            {% set dayArrive = (countdown.timearrive - Jdate.time())/86400 %}
                            <li class="rounded-0 list-group-item d-flex justify-content-between py-2 px-3 mb-1 {% if dayArrive < 10 %}bg-warning{% endif %}">
                                <div>
                                    <h4 class="my-0 text-dark" style="font-size: 1.12rem;">
                                        تا
                                        {{ countdown.title }}</h4>
                                    <small class="text-muted txt-des" style="font-size: 0.65rem;">{{ Jdate.jdate('(Y/n/d)',countdown.timearrive) }} {{ countdown.des }}</small>
                                </div>
                                <button type="button" class="btn btn-primary" style="font-size: .85rem;">
                                    {% if dayArrive | number_format(0, '.', ',') > 0 %}
                                        {{ dayArrive | number_format(0, '.', ',') }}
                                        روز
                                    {% elseif dayArrive | number_format(0, '.', ',') == 0 %}
                                        امروز
                                    {% else %}
                                        {{ dayArrive | number_format(0, '.', ',') }}
                                        روز پیش
                                    {% endif %}
                                </button>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

        <div class="row" style="margin: -9rem 5rem 3rem 5rem;">
            <div class="col-6 pr-0 pl-5">
                <div class="row">
                    <div class="col-12">
                        <div class="list-group shadow">
                            <a href="{{ path('apps') }}" class="list-group-item text-decoration-none active" style="padding: .5rem 1.25rem;">
                                <i class="fas fa-bars"></i>
                                سامانه‌ها
                            </a>
                            <div class="row z-index-999">
                                <div class="col">
                                    <div id="samane" class="row form-group m-0">
                                        {% set menuItems = EntityMGR.findAll('App:SysMenuItem') %}
                                        {% for menuItem in menuItems %}
                                            <a href="{{ menuItem.url | raw}}" class="list-group-item list-group-item-action col-6 rounded-0">
                                                {{ menuItem.fontawsome | raw }}
                                                {{ menuItem.label | raw}}
                                            </a>
                                        {% endfor %}
                                        <a href="{{ path('suggestion') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                            <i class="far fa-lightbulb"></i>
                                            نظام پیشنهادات و انتقادات
                                        </a>
                                        <a href="{{ path('ISOformsView') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                            <span class="badge badge-danger">آزمایشی</span>
                                            <i class="far fa-books"></i>
                                            سامانه IMS
                                        </a>
                                        <a href="{{ path('phonebook',{'msg':0,'search':0}) }}" class="list-group-item list-group-item-action col-6 rounded-0" style="height: calc(100% - 1px);">
                                            <i class="fas fa-book"></i>
                                            دفترچه تلفن
                                        </a>
                                        <a href="{{ path('mostusedfilesView') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                            <i class="fas fa-box-alt"></i>
                                            فرم های پرکاربرد
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4 pl-0">
                <div class="row">
                    <div class="col-12">
                        <div class="card z-index-999 shadow border-0">
                            {% if UserMGR.isLogedIn() %}
                                {% set area = EntityMGR.find('App:SysArea',UserMGR.currentPosition().DefaultArea) %}
                                <div class="card-header text-center bg-success text-light">
                                    <i class="fas fa-columns"></i>
                                    ناحیه کاری: {{ area.areaName }}
                                </div>
                                <div id="nahye">
                                    {#
                                        {% if UserMGR.hasPermission('ictRequest','ICT',null,area.id) == true %}
                                            <a href="{{ path('ictreqDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0 border-top-0">
                                                <i class="fas fa-desktop"></i>
                                                درخواست خدمات فناوری اطلاعات
                                            </a>
                                        {% endif %}
                                        {% if UserMGR.hasPermission('ictDoing','ICT',null,area.id) == true %}
                                            <a href="{{ path('ictDoingDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                                <i class="fas fa-server"></i>
                                                مجری خدمات فناوری اطلاعات
                                            </a>
                                        {% endif %}
                                    #}

                                    {% if UserMGR.hasPermission('CeremonailMNGDashboard','CEREMONIAL',null,area.id) == true %}
                                        <a href="{{ path('ceremonialDOINGDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                            <span class="badge badge-danger">آزمایشی</span>
                                            سامانه مدیریت تشریفات
                                        </a>
                                    {% endif %}
                                    {% if UserMGR.hasPermission('CeremonailREQ','CEREMONIAL',null,area.id) == true %}
                                        <a href="{{ path('ceremonialREQDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                            <span class="badge badge-danger">آزمایشی</span>
                                            سامانه درخواست تشریفات
                                        </a>
                                    {% endif %}
                                    {#
                                        {% if UserMGR.hasPermission('ceremonialHotelingOPT','CEREMONIAL',null,area.id) == true %}
                                            <a href="{{ path('hotelingOPTDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                                <i class="fas fa-hotel"></i>
                                                سامانه اپراتوری اسکان و اقامت
                                            </a>
                                        {% endif %}
                                        {% if UserMGR.hasPermission('CMOPTARBAEIN','CMARBAEIN',null,area.id) == true %}
                                            <a href="{{ path('cmarbainDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                                <i class="fas fa-circle"></i>
                                                سامانه اپراتوری اربعین
                                            </a>
                                        {% endif %}
                                    #}

                                </div>
                            {% else %}
                                <div class="card-header text-center bg-success text-light">
                                    <i class="fas fa-question-square" onclick="SysLoadMsg('1002');"></i>
                                    ورود به پورتال
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-sm-12 col-md-12 justify-content-center">
                                            <div class="container">
                                                <div class="row">
                                                    <div class="col-12">
                                                        {% block alert %}
                                                            {% if alert is defined %}
                                                                {% if alert|length != 0 %}
                                                                    {% for alt in alert %}
                                                                        <div class="alert alert-{{ alt.type }} mt-3" role="alert">
                                                                            {{ alt.message }}
                                                                        </div>
                                                                    {% endfor %}
                                                                {% endif %}
                                                            {% endif %}
                                                        {% endblock %}
                                                    </div>
                                                </div>
                                            </div>
                                            {{ form_start(form) }}
                                            <div class="input-group mb-3">
                                                <div class="input-group-prepend">
                                                    <div class="input-group-text">
                                                        <i class="fas fa-user"></i>
                                                    </div>
                                                </div>
                                                {{ form_widget(form.username) }}
                                            </div>
                                            <div class="input-group mb-3">
                                                <div class="input-group-prepend">
                                                    <div class="input-group-text">
                                                        <i class="fas fa-key"></i>
                                                    </div>
                                                </div>
                                                {{ form_widget(form.password) }}
                                                <div class="input-group-append">
                                                  <span class="input-group-text" onclick="SysLoadMsg('1003');">
                                                      <i class="fa fa-question-circle"></i>
                                                  </span>
                                                </div>
                                            </div>
                                            <br>

                                            {{ form_widget(form.submit) }}
                                            {{ form_end(form) }}
                                        </div>
                                    </div>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% if UserMGR.isLogedIn() %}
        <div class="col-2">
            <div class="card">
                <div class="card-header text-center bg-success text-light">اطلاعات کاربر</div>
                <div class="card-body">
                    <img class="img-thumbnail">
                </div>
            </div>
        </div>
        {% endif %}

    </div>



    <div class="d-block d-md-none pt-3">          {# XS-SM-MD screens #}

        <div class="row">
            <div class="col-12">
                <div class="card mb-3 shadow">
                    <div class="card-header py-2 px-3 bg-primary text-right text-white" style="font-size: .75rem;">
                        {% set countdowns = EntityMGR.findAll('App:Countdown') %}
                        <span class="fa fa-clock"></span>
                        امروز
                        <span>{{ Jdate.jdate('l , d/F/Y','now'|date('U')) }}</span>
                    </div>
                    <div class="card-body p-3">
                        <ul class="list-group row m-0 p-0">
                            {% for countdown in countdowns %}
                                {% set dayArrive = (countdown.timearrive - Jdate.time())/86400 %}
                                <li class="rounded-0 list-group-item col-12 col-sm-6 d-flex justify-content-between py-2 px-3 mb-1 {% if dayArrive < 10 %}bg-warning{% endif %}">
                                    <div>
                                        <h4 class="my-0 text-dark" style="font-size: 1.12rem;">
                                            تا
                                            {{ countdown.title }}</h4>
                                        <small class="text-muted txt-des" style="font-size: 0.65rem;">{{ Jdate.jdate('(Y/n/d)',countdown.timearrive) }} {{ countdown.des }}</small>
                                    </div>
                                    <button type="button" class="btn btn-primary" style="font-size: .85rem;">
                                        {% if dayArrive | number_format(0, '.', ',') > 0 %}
                                            {{ dayArrive | number_format(0, '.', ',') }}
                                            روز
                                        {% elseif dayArrive | number_format(0, '.', ',') == 0 %}
                                            امروز
                                        {% else %}
                                            {{ dayArrive | number_format(0, '.', ',') }}
                                            روز پیش
                                        {% endif %}
                                    </button>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>


        <div class="row mb-4">
            <div class="col-12 mb-3">
                <div class="card z-index-999 shadow border-0">
                    {% if UserMGR.isLogedIn() %}
                        {% set area = EntityMGR.find('App:SysArea',UserMGR.currentPosition().DefaultArea) %}
                        <div class="card-header text-center bg-success text-light">
                            <i class="fas fa-columns"></i>
                            ناحیه کاری: {{ area.areaName }}
                        </div>
                        <div id="nahye">
                            {% if UserMGR.hasPermission('ictRequest','ICT',null,area.id) == true %}
                                <a href="{{ path('ictreqDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0 border-top-0">
                                    <i class="fas fa-desktop"></i>
                                    درخواست خدمات فناوری اطلاعات
                                </a>
                            {% endif %}
                            {% if UserMGR.hasPermission('ictDoing','ICT',null,area.id) == true %}
                                <a href="{{ path('ictDoingDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                    <i class="fas fa-server"></i>
                                    مجری خدمات فناوری اطلاعات
                                </a>
                            {% endif %}
                            {% if UserMGR.hasPermission('CeremonailMNGDashboard','CEREMONIAL',null,area.id) == true %}
                                <a href="{{ path('ceremonialDOINGDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                    <i class="fas fa-passport"></i>
                                    سامانه مدیریت تشریفات ناحیه‌‌ای
                                </a>
                            {% endif %}
                            {% if UserMGR.hasPermission('CeremonailREQ','CEREMONIAL',null,area.id) == true %}
                                <a href="{{ path('ceremonialREQDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                    <i class="fas fa-passport"></i>
                                    سامانه درخواست تشریفات
                                </a>
                            {% endif %}
                            {% if UserMGR.hasPermission('ceremonialHotelingOPT','CEREMONIAL',null,area.id) == true %}
                                <a href="{{ path('hotelingOPTDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                    <i class="fas fa-hotel"></i>
                                    سامانه اپراتوری اسکان و اقامت
                                </a>
                            {% endif %}
                            {% if UserMGR.hasPermission('CMOPTARBAEIN','CMARBAEIN',null,area.id) == true %}
                                <a href="{{ path('cmarbainDashboard') }}" class="list-group-item list-group-item-action py-2 border-left-0 border-right-0">
                                    <i class="fas fa-circle"></i>
                                    سامانه اپراتوری اربعین
                                </a>
                            {% endif %}
                        </div>
                    {% else %}
                        <div class="card-header text-center bg-success text-light">
                            <i class="fas fa-question-square" onclick="SysLoadMsg('1002');"></i>
                            ورود به پورتال
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-sm-12 col-md-12 justify-content-center">
                                     {{ form_start(form) }}
                                    <div class="input-group mb-3">
                                        <div class="input-group-prepend">
                                            <div class="input-group-text">
                                                <i class="fas fa-user"></i>
                                            </div>
                                        </div>
                                         {{ form_widget(form.username) }}
                                    </div>
                                    <div class="input-group mb-3">
                                        <div class="input-group-prepend">
                                            <div class="input-group-text">
                                                <i class="fas fa-key"></i>
                                            </div>
                                        </div>
                                         {{ form_widget(form.password) }}
                                        <div class="input-group-append">
                                                    <span class="input-group-text" onclick="SysLoadMsg('1003');">
                                                        <i class="fa fa-question-circle"></i>
                                                    </span>
                                        </div>
                                    </div>
                                    <br>

                                    <inpu></inpu>
                                     {{ form_widget(form.submit) }}
                                    <a class="btn btn-primary" href="{{ path('home') }}">
                                        ورود به پورتال
                                    </a>
                                     {{ form_end(form) }}
                                </div>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
            <div class="col-12">
                <div class="list-group shadow">
                    <a href="{{ path('apps') }}" class="list-group-item text-decoration-none active" style="padding: .5rem 1.25rem;">
                        <i class="fas fa-bars"></i>
                        سامانه‌ها
                    </a>
                    <div class="row z-index-999">
                        <div class="col">
                            <div id="samane" class="row form-group m-0">
                                {% set menuItems = EntityMGR.findAll('App:SysMenuItem') %}
                                {% for menuItem in menuItems %}
                                    <a href="{{ menuItem.url | raw}}" class="list-group-item list-group-item-action col-6 rounded-0">
                                        {{ menuItem.fontawsome | raw }}
                                        {{ menuItem.label | raw}}
                                    </a>
                                {% endfor %}
                                <a href="{{ path('suggestion') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                    <i class="far fa-lightbulb"></i>
                                    نظام پیشنهادات و انتقادات
                                </a>
                                <a href="{{ path('ISOformsView') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                    <i class="far fa-books"></i>
                                    سامانه IMS
                                </a>
                                <a href="{{ path('phonebook',{'msg':0,'search':0}) }}" class="list-group-item list-group-item-action col-6 rounded-0" style="height: calc(100% - 1px);">
                                    <i class="fas fa-book"></i>
                                    دفترچه تلفن
                                </a>
                                <a href="{{ path('mostusedfilesView') }}" class="list-group-item list-group-item-action col-6 rounded-0">
                                    <i class="fas fa-box-alt"></i>
                                    فرم های پرکاربرد
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {#
              {% if UserMGR.isLogedIn() %}
                <hr class="d-none">
              {% else %}
                <hr class="d-none d-md-block" style="margin: 0 7rem 2rem 7rem;">
              {% endif %}
        #}
    </div>

    {% if UserMGR.isLogedIn() %}
    {% else %}
        <p class="h5 text-muted mb-5 text-center d-none d-md-block">اخبار و نمودار پیشرفت پروژه ها</p>
    {% endif %}


    <div class="row">
        <div class="col-12 col-md-4 px-0 px-md-3 mb-3">
            <div class="col px-3 px-md-0">
                <div class="card">
                    <div class="card-header text-center bg-primary text-light">
                        تابلو اعلانات
                    </div>
                    <div class="card-body">
                        <ul class="list-unstyled">
                            {% set posts = EntityMGR.findByPage('App:NewsPost',1,10) %}
                            {% for post in posts %}
                                <li><a href="{{ path('newsShowPost',{'id':post.id}) }}" class="text-decoration-none d-inline-block py-1 move-3 link-hover"><i class="fas fa-angle-left text-dark small pl-1"></i><span>{{ post.title }}</span></a></li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        {#
              {% if UserMGR.isLogedIn() %}
              <div class="col-12 col-md-9 m-auto">
              {% else %}
        #}
        <div class="col-12 col-sm-12 col-md-8">
            {#      {% endif %}#}
            {% if breadcrumb is not defined %}
                <ol class="breadcrumb my-2">
                    <li class="breadcrumb-item"><a class="" href="{{ path('home') }}">
                            <i class="fas fa-home"></i>
                            پورتال اصلی
                        </a></li>
                    <li class="breadcrumb-item"><a class="" href="{{ path('apps') }}">
                            <i class="fal fa-window-alt"></i>
                            برنامه‌ها
                        </a>
                    </li>
                    <li class="breadcrumb-item active">{{ block('title') }}</li>
                </ol>
            {% endif %}

                <div class="row mb-2">
                    <div class="col-12 col-sm-12 col-md-12 pb-2 d-none d-sm-block">
                        <div class="row">
                            <div class="col-12 col-md-12">
                                <div class="card mb-2">
                                    <div class="card-header">
                                        <i class="fas fa-question-square" onclick="SysLoadMsg('1001');"></i>
                                        نمودار پیشرفت
                                        :
                                        <span class="text-secondary projectsLabel"></span>
                                        <script>
                                            $(document).ready(function(){
                                                var typed = new Typed('.projectsLabel', {
                                                    strings: {{ pn | json_encode | raw }},
                                                    typeSpeed: 30,
                                                    loop: true
                                                });
                                            });
                                        </script>
                                    </div>
                                    <div class="card-body pb-1">
                                        <div class="chart-container">
                                            <canvas id="myChart"></canvas>
                                        </div>
                                        <script>

                                            var ctx = document.getElementById('myChart');
                                            var color = Chart.helpers.color;
                                            Chart.defaults.global.defaultFontFamily = 'Shabnam FD';
                                            window.chartColors = {
                                                red: 'rgb(255, 99, 132)',
                                                orange: 'rgb(255, 159, 64)',
                                                yellow: 'rgb(255, 205, 86)',
                                                green: 'rgb(75, 192, 192)',
                                                blue: 'rgb(54, 162, 235)',
                                                purple: 'rgb(153, 102, 255)',
                                                grey: 'rgb(201, 203, 207)'
                                            };
                                            var myChart = new Chart(ctx, {
                                                type: 'bar',
                                                data: {
                                                    labels: {{ pn | json_encode | raw }},
                                                    datasets: [
                                                        {
                                                            label: 'پیشرفت برنامه‌ای',
                                                            backgroundColor: color(window.chartColors.red).alpha(0.5).rgbString(),
                                                            borderColor: window.chartColors.red,
                                                            borderWidth: 1,
                                                            data: {{ cp | json_encode | raw }},
                                                        },
                                                        {
                                                            label: 'پیشرفت فیزیکی',
                                                            backgroundColor: color(window.chartColors.blue).alpha(0.5).rgbString(),
                                                            borderColor: window.chartColors.blue,
                                                            borderWidth: 1,
                                                            data: {{ pp | json_encode | raw}},
                                                        }

                                                    ]
                                                },
                                                options: {
                                                    scales: {
                                                        yAxes: [{
                                                            ticks: {
                                                                beginAtZero: true
                                                            }
                                                        }],
                                                        xAxes: [{
                                                            barPercentage: 0.5,
                                                            barThickness: 25,
                                                            maxBarThickness: 25,
                                                            minBarLength: 2,
                                                            gridLines: {
                                                                offsetGridLines: true
                                                            }
                                                        }]
                                                    },
                                                    legend: {
                                                        labels: {
                                                            // This more specific font property overrides the global property
                                                            fontColor: 'black'
                                                        }
                                                    }
                                                }
                                            });
                                        </script>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

        </div>
    </div>
{% endblock %}


