{% extends 'appSimple.html.twig' %}
{% block title %}دفترچه تلفن{% endblock %}
{% block appBody %}
    <div class="card">
        <div class="card-header bg-info text-light">
            دفترچه تلفن
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            <div class="row">
                <div class="col-9 col-md-6 pb-1 pl-0">
                    {{ form_widget(form.search) }}
                </div>
                <div class="col-3 col-md-2 pb-1 pl-3 pl-sm-0 pr-0 rounded-left">
                    {{ form_widget(form.submit) }}
                </div>
                <div class="col-12 col-md-4 text-left pb-1">
                    {% if UserMGR.hasPermission('phonebookAdmin','PHONEBOOK') == true %}
                        <a class="btn btn-warning" href="{{ path('phonebookAddNew') }}">
                            <i class="fas fa-user-plus"></i>
                            شماره جدید
                        </a>
                    {% endif %}
                </div>
            </div>
            {{ form_end(form) }}
            <hr class="mt-0">
            <div class="table-responsive">
                <table class="table table-sm table-bordered table-hover rounded text-center">
                    <thead class="bg-blue text-light">
                    <tr>
                        <th>ردیف</th>
                        <th>نام و نام خانوادگی</th>
                        <th>شماره تلفن</th>
                        <th>داخلی</th>
                        <th>موبایل</th>
                        <th>توضیحات</th>
                        <th>امکانات</th>
                    </tr>
                    </thead>
                    <tbody class="">
                    {% for num in nums %}
                        <tr class="">
                            <td>{{ loop.index }}</td>
                            <td>{{ num.username }}</td>
                            <td style="direction: ltr">{{ num.tel1 }}</td>
                            <td style="direction: ltr">{{ num.tel2 }}</td>
                            <td style="direction: ltr">{{ num.mobile1 }}</td>
                            <td style="direction: ltr">{{ num.des }}</td>
                            <td>
                                <a class="btn btn-block btn-primary btn-sm rounded" href="{{ path('phonebookView',{'id':num.id}) }}">نمایش</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
