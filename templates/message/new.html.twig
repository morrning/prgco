{% extends 'appSimple.html.twig' %}
{% block title %}پیام جدید{% endblock %}
{% block appBody %}
<div class="card">
    <div class="card-header bg-info text-light">
        ارسال پیام جدید
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-sm-12">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}